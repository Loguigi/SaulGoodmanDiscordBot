CREATE TABLE [dbo].[Wheels] (
    [GuildId]     BIGINT          NOT NULL,
    [WheelName]   NVARCHAR(100)   NOT NULL,
    [WheelOption] NVARCHAR(100)   NOT NULL,
    [ImageUrl]    NVARCHAR(1000)  NULL
);